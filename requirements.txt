# Checkov's own runtime dependencies — added to Repo A so Snyk has a
# manifest to scan. These are the same packages that Repo B installs
# on the runner via its requirements.txt, but Snyk can only see them
# here because this file lives in the caller's workspace.

requests==2.28.1
PyYAML==6.0
Jinja2==3.1.2
boto3==1.26.76
urllib3==1.26.14
